---
'@sveltejs/vite-plugin-svelte': minor
---

feat: add `compiler` option to use an alternative svelte compiler module instead of `svelte/compiler`

Note: after upgrading, the prebundle metadata format has changed, so dependencies will be re-optimized once on the next dev server start.
